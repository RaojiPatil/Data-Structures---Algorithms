var arr = [4, 5, 3, 7, 1];
var c = arr;
var out = "";
console.log(c);
if(call(index) == c[i]) {
    out = out + i;
}
arr.sort(function (a, b) {return a - b});

var index = "";
function call(arr) {

for(var i = 0; i < arr.length; i++) {
   if(arr[i] == c[i]) {
       index = index + i + " ";
   }
} return index;
}
// console.log(index);